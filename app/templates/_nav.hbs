<nav>
  <LinkTo @route="index" @query={{hash siteId=this.model.siteId}} class="main">
    <img src={{this.model.logoUrl}} alt="Site logo">
  </LinkTo>
  <div class="menu">
    <Input @value={{this.tempQuery}} />
    <button type="button" {{action "navSearch" this.tempQuery}}>
      Search
    </button>
    <button type="button" {{action "watch"}}>
      Watch Live
    </button>
  </div>
</nav>